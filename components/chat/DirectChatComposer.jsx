import React from "react";

export default function DirectChatComposer({ image, onClearImage, actionPanelOpen, setActionPanelOpen, allowTransfer, onOpenTransfer, fileInputRef, onImageUpload, value, setValue, textLimit, onSend, tr }) {
  return <>{image && <div className="mp-imgprev"><img src={`data:${image.mime};base64,${image.data}`} alt="" /><div style={{ fontSize: 10, color: "var(--mp-txt-l)", marginTop: 4 }}>{image.width}x{image.height} · {Math.round(image.bytes / 1024)}KB</div><button onClick={onClearImage}>×</button></div>}{actionPanelOpen && <div className="mp-chat-actions"><button className="mp-chat-action" onClick={() => { setActionPanelOpen(false); fileInputRef.current?.click(); }}><span className="mp-chat-action-i">🖼</span><span>{tr("相片", "Photo", "写真", "사진")}</span></button>{allowTransfer && <button className="mp-chat-action" onClick={() => { setActionPanelOpen(false); onOpenTransfer(); }}><span className="mp-chat-action-i">💸</span><span>{tr("轉帳", "Transfer", "送金", "송금")}</span></button>}<button className="mp-chat-action" disabled><span className="mp-chat-action-i">📅</span><span>{tr("日程", "Schedule", "予定", "일정")}</span></button><button className="mp-chat-action" disabled><span className="mp-chat-action-i">⚙️</span><span>{tr("更多", "More", "その他", "더보기")}</span></button></div>}<div className="mp-inp-bar"><button className={`mp-btn mp-btn-img ${actionPanelOpen ? "active" : ""}`} onClick={() => setActionPanelOpen((open) => !open)}>＋</button><input type="file" ref={fileInputRef} accept="image/*" style={{ display: "none" }} onChange={onImageUpload} /><div className="mp-inp-wrap"><textarea className="mp-inp" placeholder={tr("輸入訊息...", "Type a message...", "メッセージを入力...", "메시지를 입력...")} name="mali_chat_text" rows={1} maxLength={textLimit} autoComplete="off" autoCorrect="off" autoCapitalize="sentences" spellCheck={false} data-form-type="other" data-lpignore="true" value={value} onChange={(event) => setValue(event.target.value.slice(0, textLimit))} /><div className="mp-char-counter">{value.length}/{textLimit}</div></div><button className="mp-btn mp-btn-send" onClick={onSend}>➤</button></div></>;
}
