import React from "react";

export default function ProactiveMessageSettings({ enabled, frequency, onToggle, onFrequencyChange, tr }) {
  return <div className="mp-cc"><div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12 }}><div style={{ minWidth: 0 }}><div style={{ fontSize: 13, fontWeight: 700 }}>{tr("主動傳訊息", "Proactive messages", "自発的なメッセージ", "먼저 보내는 메시지")}</div><div style={{ fontSize: 10, color: "var(--mp-txt-l)", marginTop: 3, lineHeight: 1.5 }}>{tr("離開一段時間再回來時，角色有機會主動傳訊息給你。", "When you're away and come back, the character may occasionally message you first.", "しばらく離れてから戻ると、キャラが時々先にメッセージを送ることがあります。", "자리를 비웠다 돌아오면 캐릭터가 가끔 먼저 메시지를 보낼 수 있습니다.")}</div></div><button type="button" role="switch" aria-checked={enabled} className={`mp-switch ${enabled ? "active" : ""}`} onClick={onToggle}><span /></button></div>{enabled && <div className="mp-mode-tabs" style={{ marginTop: 10, gridTemplateColumns: "repeat(3,1fr)" }}>{[["low", tr("低頻率", "Low", "低頻度", "낮음")], ["normal", tr("一般", "Normal", "普通", "보통")], ["high", tr("高頻率", "High", "高頻度", "높음")]].map(([value, label]) => <button type="button" key={value} className={`mp-mode-tab ${frequency === value ? "active" : ""}`} onClick={() => onFrequencyChange(value)}>{label}</button>)}</div>}</div>;
}
