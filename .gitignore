node_modules
dist
.DS_Store
Thumbs.db
Desktop.ini
*.log
logs/
coverage/
.vite/
.cache/
*.tsbuildinfo

# Environment files and local credentials (examples remain shareable)
.env
.env.*
**/.env
**/.env.*
!.env.example
!**/.env.example

# Certificates, mobile signing files, Firebase/service-account credentials and local deployment secrets
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.mobileprovision
**/google-services.json
**/GoogleService-Info.plist
**/service-account*.json
**/*serviceAccount*.json
**/credentials*.json
.dev.vars
**/.dev.vars
.envrc
*.tfvars
secrets/
*.secrets.*

# Build packages and local database files must never be published as source
*.apk
*.aab
*.ipa
*.db
*.sqlite
*.sqlite3

# Actual Cloudflare deployment binding IDs stay local; commit only the example.
cloudflare-worker-proxy/wrangler.toml

# Local Codex/editor workspace state
.local/
.codex-remote-attachments/
android/.idea/
/android/
/backend/
backend/.env
backend/.env.*
!backend/.env.example
backend/node_modules/
backend/public/
.pglite-dev/

# Local prototypes that are not wired into the production app yet
/GachaGame.jsx
card/
chat-layout-preview.html
chat-layout-preview.css

# Private Yunyin assets stay out of Git
yunyin/assets/
# Local-only content editors and their drafts
tools/gacha-card-editor/
tools/dating-profile-editor/
tools/dating-profile-editor.zip
# Local-only assets destined for separate private asset repositories.
# Normal product UI assets such as public/app-icons/ remain tracked by MAIP.
private-assets/

# Local image-generation working files and exports
/output/imagegen/
/tmp/imagegen/
/public/gacha/
/assets/card-frames/
/assets/cards/preview/

# Local developer config / internal planning docs
.claude/
backend/docs/plan.md
/docs/
/tmp/

# 資安檢查紀錄：記載尚未修復的弱點，repo 是 public 的，外流等於給攻擊者地圖
# Covered by /docs/ above.

# 付費素材原始檔：授權禁止散布，repo 是 public 的所以不能進 git（build 產物 dist/ 已排除，不受影響）
yunyin/assets/
