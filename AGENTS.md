# MAIPHONE 協作備註

## 發布方式

- 使用者要求「PUSH」或「推送更新」時，不要因為電腦未安裝 GitHub CLI（`gh`）而中止或要求安裝。
- 在本專案中，單純的「PUSH」表示以標準 Git 完成目前變更的提交與推送，不代表要執行包含 Pull Request 的完整 GitHub 發布流程。
- 本專案使用既有的 Git 遠端與認證進行提交及推送，例如 `git push origin <目前分支>`；GitHub CLI 不是發布的必要條件。
- 只有在使用者明確要求建立、查詢或管理 Pull Request，而且現有 GitHub 連線工具無法完成時，才考慮使用 `gh`。
- 推送前仍需確認變更範圍、寫入當次更新日記，並完成適合的測試與正式建置。
